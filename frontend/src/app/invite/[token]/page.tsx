"use client";
import { useEffect, useState } from "react";
import { api } from "@/lib/api";
import { authClient } from "@/lib/auth-client";
export default function InvitePage({ params }: { params: Promise<{ token: string }> }) { const [token, setToken] = useState(""); const [invite, setInvite] = useState<any>(); const [name, setName] = useState(""); const [password, setPassword] = useState(""); const [error, setError] = useState(""); const [hasSession, setHasSession] = useState(false); useEffect(() => { void authClient.getSession().then(result => setHasSession(Boolean(result.data?.user))); params.then(({ token }) => { setToken(token); return api.get(`/auth/invitations/${token}`); }).then(response => setInvite(response.data)).catch(e => setError(e.response?.data?.error || "Einladung ungültig.")); }, [params]); const accept = async (event: React.FormEvent) => { event.preventDefault(); try { await api.post(`/auth/invitations/${token}/accept`, { name, password }); const result = await authClient.signIn.email({ email: invite.email, password }); if (result.error) throw new Error(result.error.message || "Anmeldung fehlgeschlagen."); window.location.assign("/"); } catch (e: any) { setError(e.response?.data?.error || e.message || "Einladung konnte nicht angenommen werden."); } }; const acceptExisting = async () => { try { await api.post(`/auth/invitations/${token}/accept-existing`); window.location.assign("/"); } catch (e: any) { setError(e.response?.data?.error || "Einladung konnte nicht angenommen werden."); } }; return <main className="grid min-h-screen place-items-center bg-slate-950 p-6 text-white">{error ? <p>{error}</p> : !invite ? <p>Einladung wird geprüft …</p> : hasSession ? <section className="w-full max-w-md space-y-4 rounded-xl bg-slate-900 p-8"><h1 className="text-xl font-bold">Einladung annehmen</h1><p className="text-sm text-slate-400">Angemeldet als vorhandenes Konto. Die E-Mail-Adresse muss mit {invite.email} übereinstimmen.</p><button onClick={acceptExisting} className="w-full rounded bg-blue-600 p-3">Organisation beitreten</button></section> : <form onSubmit={accept} className="w-full max-w-md space-y-4 rounded-xl bg-slate-900 p-8"><h1 className="text-xl font-bold">Einladung annehmen</h1><p className="text-sm text-slate-400">{invite.email} · {invite.role}</p><input required value={name} onChange={e => setName(e.target.value)} placeholder="Name" className="w-full rounded bg-slate-800 p-3" /><input required minLength={12} type="password" value={password} onChange={e => setPassword(e.target.value)} placeholder="Passwort" className="w-full rounded bg-slate-800 p-3" /><button className="w-full rounded bg-blue-600 p-3">Konto erstellen und beitreten</button></form>}</main>; }
